name: Sincronizacion Automatica desde SharePoint

on:
  repository_dispatch:
    types: [sharepoint_sync, update_estudios]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Guardar archivo modificado desde el Payload
        env:
          FILE_PATH: ${{ github.event.client_payload.file_path }}
          FILE_NAME: ${{ github.event.client_payload.file_name }}
          FILE_CONTENT_BASE64: ${{ github.event.client_payload.file_content }}
        run: |
          node sync_file_from_payload.js

      - name: Recompilar Portal Modular
        run: |
          node build_modular_site.js
          node copy_dist_to_root.js

      - name: Confirmar y Publicar cambios en la rama main
        run: |
          git config --global user.name "CION Automation Bot"
          git config --global user.email "cion_sunass@sunass.gob.pe"
          git add index.html dist/ eps/ *.html
          if git diff --staged --quiet; then
            echo "No hay cambios pendientes en los archivos."
          else
            git commit -m "Auto-Sync: Actualizacion automatica de archivo desde OneDrive (${{ github.event.client_payload.file_name }})"
            git push origin main
          fi
